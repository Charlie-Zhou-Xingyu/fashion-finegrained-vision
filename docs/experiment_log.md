### Current Status

This experiment is kept as a possible segmentation quality evaluation plan.

However, the current project has already implemented an end-to-end YOLO + SAM-HQ + landmark + region crop pipeline. Therefore, the immediate next priority is no longer a standalone SAM-HQ GT-box evaluation, but:

1. FashionAI attribute classification baseline.
2. Region localization + attribute classification integration.
3. YOLO-seg baseline for replacing or accelerating SAM-HQ.
